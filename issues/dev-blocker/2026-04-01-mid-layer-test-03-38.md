# 中层测试报告 2026-04-01 03:38

## 执行摘要

| 项目 | 数量 |
|------|------|
| 扫描PR总数 | 5个 (backend) |
| 测试通过 | 0个 |
| 测试失败 | 5个（环境阻塞） |
| 阻塞原因 | Dev环境服务启动失败 |

---

## PR测试详情

### Backend PR（全部阻塞）

| PR | Issue | 标题 | 状态 | 失败原因 |
|----|-------|------|------|----------|
| #912 | #698 | CC API调用质量监控 | ❌ 阻塞 | Dev环境服务未启动 |
| #906 | #698 | CC API调用质量监控 | ❌ 阻塞 | Dev环境服务未启动 |
| #905 | #567 | 工具管理Service+API | ❌ 阻塞 | Dev环境服务未启动 |
| #850 | #485 | 开发阻塞主动提醒 | ❌ 阻塞 | Dev环境服务未启动 |
| #839 | #309 | Dealer Phase3模块联动 | ❌ 阻塞 | Dev环境服务未启动 |

---

## 阻塞根因

**Dev环境后端服务启动失败**

错误信息：
```
org.apache.ibatis.type.TypeException: The alias 'UserFeedback' is already mapped
to the value 'org.ruoyi.wande.domain.feedback.UserFeedback'.
```

**分析**：
- MyBatisPlus 类型别名注册冲突
- `UserFeedback` 类被重复映射，可能是多个 PR 合并后产生了重复的类定义或别名配置
- 这是一个代码冲突问题，不是测试环境问题

**影响**：
- 后端服务完全无法启动（端口6040无监听）
- 所有 Backend API 测试无法执行（connection refused）
- 5个待测 PR 均无法完成 E2E 验证

---

## 测试执行结果

```
Backend API测试: 0 passed, 全部 failed (connection refused)
测试文件: tests/backend/api/*.spec.ts
总用例数: 约300+
失败原因: connect ECONNREFUSED 127.0.0.1:6040
```

---

## 建议操作

1. **编程CC紧急处理**：
   - 检查 `UserFeedback` 类的重复定义
   - 可能位置：
     - `org.ruoyi.wande.domain.feedback.UserFeedback`
     - 其他模块可能也有同名类
   - 检查 MyBatis 的 `type-aliases-package` 配置是否有重叠

2. **临时绕过方案**：
   - 如果有紧急PR需要合并，可以考虑在本地修复冲突后强制部署
   - 但建议先解决根本问题，避免技术债务

3. **测试CC策略**：
   - 保持所有5个PR的 `status:test-failed` 标签
   - 提交 `request-changes` review 说明环境阻塞
   - 等待 dev 环境修复后重新执行中层测试

---

## 相关Issue

- 需要创建 P0 Issue 跟踪此启动失败问题
- 影响范围：所有 backend PR 的测试和合并

---

*报告生成时间: 2026-04-01 03:38 UTC*
*测试CC: wande-ai-e2e*
